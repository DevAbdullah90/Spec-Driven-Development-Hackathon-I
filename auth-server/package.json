{
  "name": "auth-server",
  "version": "1.0.0",
  "description": "Better Auth server for Docusaurus project",
  "main": "server.ts",
  "scripts": {
    "dev": "tsx --watch server.ts",
    "migrate": "npx better-auth migrate",
    "generate": "npx better-auth generate"
  },
  "dependencies": {
    "@hono/node-server": "^1.0.0",
    "better-auth": "^1.0.0",
    "better-sqlite3": "^12.0.0",
    "dotenv": "^16.6.1",
    "hono": "^4.0.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0"
  }
}
